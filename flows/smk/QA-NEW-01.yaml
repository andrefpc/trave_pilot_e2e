appId: ${APP_ID}
name: QA-NEW-01
tags: [smk, p0]
---
# >>> dashboard-meta
# QA-NEW-01 — New feature smoke
# Section: Smoke Suite
# Priority: P0
# Repro:
# (no repro steps recorded)
# <<< dashboard-meta

# No repro steps recorded on the dashboard. Treat this as a baseline smoke:
# cold-launch the app from a fresh state and verify Welcome renders.
- launchApp:
    clearState: true
    clearKeychain: true
    stopApp: true
- assertVisible: { id: "welcome.title" }
