appId: ${APP_ID}
name: SMK-07
tags: [smk, p0]
---
# >>> dashboard-meta
# SMK-07 — Profile → Logout signs the user out and returns to the Welcome screen
# Section: Smoke Suite
# Priority: P0
# Repro:
# 1. Sign in, then tap the Profile tab.
# 2. Scroll down and tap "Logout".
# 3. In the confirmation sheet, tap "Log Out".
# 4. Verify Welcome appears and that Home is no longer reachable until you sign in again.
# <<< dashboard-meta

- launchApp:
    clearState: true
    clearKeychain: true
    stopApp: true
- runFlow:
    file: ../common/_login.yaml
    env:
      TEST_EMAIL: ${TEST_EMAIL}
      TEST_PASSWORD: ${TEST_PASSWORD}
- tapOn: { id: "home.tab.profile" }
- assertVisible: { id: "profile.title" }
- scrollUntilVisible:
    element: { id: "profile.logout-button" }
    direction: DOWN
- tapOn: { id: "profile.logout-button" }
- assertVisible: { id: "profile.logout-sheet.title" }
- tapOn: { id: "profile.logout-sheet.confirm-button" }
- assertVisible: { id: "welcome.title" }
- assertNotVisible: { id: "home.tab-bar" }
