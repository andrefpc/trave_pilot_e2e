appId: ${APP_ID}
name: GEN-15
tags: [needs-implementation, generation, p2]
---
# >>> dashboard-meta
# GEN-15 — While plan generation runs, a persistent low-priority notification "Creating your adventure" is visible in the tray
# Section: Plan Generation
# Priority: P2
# Repro:
# 1. Trigger plan generation.
# 2. Pull down the notification shade.
# 3. Verify a persistent low-priority notification labelled "Creating your adventure" is shown until generation finishes.
# <<< dashboard-meta

# TODO: implement interaction steps. Remove the `needs-implementation`
# tag above once the flow asserts the expected behavior end-to-end.
- runFlow: ../common/_reset_data.yaml
- runFlow: ../common/_open_app.yaml
