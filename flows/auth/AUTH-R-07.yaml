appId: ${APP_ID}
name: AUTH-R-07
tags: [needs-implementation, auth, p0]
---
# >>> dashboard-meta
# AUTH-R-07 — A password without a digit keeps the "Number" rule red and Create Account disabled
# Section: Authentication & Session
# Priority: P0
# Repro:
# 1. On Register, type "Password!" into Password and matching Confirm.
# 2. Verify the Number rule stays red and Create Account is disabled.
# <<< dashboard-meta

# TODO: implement interaction steps. Remove the `needs-implementation`
# tag above once the flow asserts the expected behavior end-to-end.
- runFlow: ../common/_reset_data.yaml
- runFlow: ../common/_open_app.yaml
