appId: ${APP_ID}
name: AUTH-F-06
tags: [auth, p0]
---
# >>> dashboard-meta
# AUTH-F-06 — Entering "00000" or any wrong code triggers an "Invalid reset token" error when you try to reset the password
# Section: Authentication & Session
# Priority: P0
# Repro:
# 1. Open Recovery Code and type "00000".
# 2. Tap the primary button and continue to Reset Password.
# 3. Type a valid new password + confirmation and tap Reset.
# 4. Verify the dialog "Invalid reset token" appears.
# <<< dashboard-meta

- launchApp:
    clearState: true
    clearKeychain: true
    stopApp: true
- assertVisible: { id: "welcome.title" }
- tapOn: { id: "welcome.have-account-login" }
- tapOn: { id: "auth-sheet.signin-with-password" }
- tapOn: { id: "login.forgot-password-link" }
- tapOn: { id: "forgot-password.email-field" }
- inputText: ${TEST_EMAIL}
- hideKeyboard
- tapOn: { id: "forgot-password.send-code-button" }
- assertVisible: { id: "recovery-code.title" }
- tapOn: { id: "recovery-code.cell.0" }
- inputText: "00000"
- tapOn: { id: "recovery-code.submit-button" }
- assertVisible: { id: "reset-password.title" }
- tapOn: { id: "reset-password.password-field" }
- inputText: "NewPass1!"
- tapOn: { id: "reset-password.confirm-password-field" }
- inputText: "NewPass1!"
- hideKeyboard
- tapOn: { id: "reset-password.submit-button" }
- assertVisible: { id: "reset-password.invalid-token-dialog.title" }
