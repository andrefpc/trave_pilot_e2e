appId: ${APP_ID}
name: ITV-04
tags: [needs-implementation, itinerary, p1]
---
# >>> dashboard-meta
# ITV-04 — Each Itinerary day shows a weather badge with the day's forecast (icon + temperature)
# Section: Itinerary Viewing
# Priority: P1
# Repro:
# 1. Open a plan with weather data.
# 2. Look at the day header.
# 3. Verify a weather icon and a temperature in °C/°F appear.
# <<< dashboard-meta

# TODO: implement interaction steps. Remove the `needs-implementation`
# tag above once the flow asserts the expected behavior end-to-end.
- runFlow: ../common/_reset_data.yaml
- runFlow: ../common/_open_app.yaml
