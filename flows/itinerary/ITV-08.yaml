appId: ${APP_ID}
name: ITV-08
tags: [needs-implementation, itinerary, p1]
---
# >>> dashboard-meta
# ITV-08 — Pulling down on the Itinerary refetches the itinerary and weather
# Section: Itinerary Viewing
# Priority: P1
# Repro:
# 1. Open a day on Itinerary.
# 2. Drag down to trigger pull-to-refresh.
# 3. Verify a loading indicator appears and the data reloads.
# <<< dashboard-meta

# TODO: implement interaction steps. Remove the `needs-implementation`
# tag above once the flow asserts the expected behavior end-to-end.
- runFlow: ../common/_reset_data.yaml
- runFlow: ../common/_open_app.yaml
