appId: ${APP_ID}
name: PLANS-06
tags: [plans, p1, ios-only]
---
# >>> dashboard-meta
# PLANS-06 — Long-press → Delete → Cancel keeps the plan untouched in the list
# Section: Travel Plans List & Plan Management
# Priority: P1
# Repro:
# 1. Long-press a plan, tap Delete.
# 2. In the confirmation, tap Cancel.
# 3. Verify no request fires and the plan still appears in the list.
# <<< dashboard-meta

- runFlow: ../common/_reset_data.yaml
- runFlow: ../common/_open_app.yaml
- runFlow:
    file: ../common/_login.yaml
    env:
      TEST_EMAIL: ${TEST_PREMIUM_EMAIL}
      TEST_PASSWORD: ${TEST_PREMIUM_PASSWORD}
- tapOn: { id: "home.tab.travel-plans" }
- assertVisible: { id: "plans.plan-card.0" }
- longPressOn: { id: "plans.plan-card.0" }
- tapOn: { id: "plans.context-menu.delete" }
- assertVisible: { id: "plans.delete-dialog" }
- tapOn: { id: "plans.delete-dialog.cancel-button" }
- assertNotVisible: { id: "plans.delete-dialog" }
- assertVisible: { id: "plans.plan-card.0" }
