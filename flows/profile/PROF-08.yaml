appId: ${APP_ID}
name: PROF-08
tags: [profile, p0]
---
# >>> dashboard-meta
# PROF-08 — Submitting Change Password with the wrong old password shows "Incorrect password" inline
# Section: Profile & Account
# Priority: P0
# Repro:
# 1. On Change Password, enter the wrong old password and a valid new password.
# 2. Tap Save.
# 3. Verify the error "Incorrect password" appears under Old Password.
# <<< dashboard-meta

- runFlow: ../common/_reset_data.yaml
- runFlow: ../common/_open_app.yaml
- runFlow:
    file: ../common/_login.yaml
    env:
      TEST_EMAIL: ${TEST_FREE_EMAIL}
      TEST_PASSWORD: ${TEST_FREE_PASSWORD}
- tapOn: { id: "home.tab.profile" }
- tapOn: { id: "profile.settings-link" }
- tapOn: { id: "settings.change-password-row" }
- assertVisible: { id: "settings.change-password.title" }
- tapOn: { id: "settings.change-password.old-password-field" }
- inputText: "WrongOldPwd!9"
- tapOn: { id: "settings.change-password.new-password-field" }
- inputText: "E2eRotated!2"
- tapOn: { id: "settings.change-password.confirm-password-field" }
- inputText: "E2eRotated!2"
- hideKeyboard
- tapOn: { id: "settings.change-password.submit-button" }
- waitForAnimationToEnd:
    timeout: 5000
- assertVisible: { id: "settings.change-password.old-password-field.error" }
