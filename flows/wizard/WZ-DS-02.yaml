appId: ${APP_ID}
name: WZ-DS-02
tags: [needs-implementation, wizard, p1]
---
# >>> dashboard-meta
# WZ-DS-02 — Tapping the "Start of the day" field on a Day Schedule card opens a 24-hour Material3 time picker
# Section: Plan Creation Wizard
# Priority: P1
# Repro:
# 1. On Day Schedule, tap a day card's "Start of the day" field.
# 2. Verify a Material3 time picker opens in 24-hour mode.
# 3. Pick e.g. 08:00 → tap OK and verify the field shows 08:00.
# <<< dashboard-meta

# TODO: implement interaction steps. Remove the `needs-implementation`
# tag above once the flow asserts the expected behavior end-to-end.
- runFlow: ../common/_reset_data.yaml
- runFlow: ../common/_open_app.yaml
