appId: ${APP_ID}
name: WZ-CO-03
tags: [wizard, p0]
---
# >>> dashboard-meta
# WZ-CO-03 — On Complete Onboarding, tapping Create Account opens Register with the wizard's draftId attached
# Section: Plan Creation Wizard
# Priority: P0
# Repro:
# 1. From Complete Onboarding (signed out), tap "Create Account".
# 2. Verify Register opens; the URL/route includes the draftId from the wizard.
# 3. Completing registration takes you to Plan Generation for that same draft.
# <<< dashboard-meta

- runFlow: ../common/_reset_data.yaml
- runFlow: ../common/_open_app.yaml
- assertVisible: { id: "welcome.title" }
- tapOn: { id: "welcome.create-plan-button" }
- runFlow: _walk_to_complete_onboarding.yaml
- assertVisible: { id: "wizard.complete-onboarding.title" }
# Tap Generate Plan to open the SignUpBottomSheet hosting the auth
# CTAs, then tap Create Account inside the sheet.
- tapOn: { id: "wizard.complete-onboarding.generate-plan-button" }
- extendedWaitUntil:
    visible: { id: "wizard.complete-onboarding.create-account-button" }
    timeout: 5000
- tapOn: { id: "wizard.complete-onboarding.create-account-button" }
- assertVisible: { id: "register.title" }
- assertVisible: { id: "register.draft-id-marker" }
- tapOn: { id: "register.name-field" }
- inputText: ${TEST_NEW_NAME}
- tapOn: { id: "register.email-field" }
- inputText: ${TEST_NEW_EMAIL}
- tapOn: { id: "register.password-field" }
- inputText: ${TEST_NEW_PASSWORD}
- tapOn: { id: "register.confirm-password-field" }
- inputText: ${TEST_NEW_PASSWORD}
- runFlow: ../common/_hide_keyboard.yaml
- tapOn: { id: "register.create-account-button" }
- assertVisible: { id: "plan-generation.progress" }
- assertNotVisible: { id: "home.tab-bar" }
