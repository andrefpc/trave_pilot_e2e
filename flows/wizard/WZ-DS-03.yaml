appId: ${APP_ID}
name: WZ-DS-03
tags: [wizard, p1]
---
# >>> dashboard-meta
# WZ-DS-03 — Tapping the "End of the day" field on a Day Schedule card opens a 24-hour time picker
# Section: Plan Creation Wizard
# Priority: P1
# Repro:
# 1. On Day Schedule, tap a day card's "End of the day" field.
# 2. Verify a Material3 time picker opens.
# 3. Pick e.g. 22:00 → tap OK and verify the field shows 22:00.
# <<< dashboard-meta

- runFlow: ../common/_reset_data.yaml
- runFlow: ../common/_open_app.yaml
- assertVisible: { id: "welcome.title" }
- tapOn: { id: "welcome.create-plan-button" }
- runFlow: ../common/_wizard_fill_destination.yaml
- assertVisible: { id: "wizard.walking-style.title" }
- tapOn: { id: "wizard.walking-style.option.moderate" }
- tapOn: { id: "wizard.walking-style.next-button" }
- assertVisible: { id: "wizard.companions.title" }
- tapOn: { id: "wizard.companions.next-button" }
- assertVisible: { id: "wizard.day-schedule.title" }
- tapOn: { id: "wizard.day-schedule.day-card.0.end-field" }
- assertVisible: { id: "wizard.day-schedule.time-picker" }
- tapOn: { id: "wizard.day-schedule.time-picker.ok-button" }
- assertNotVisible: { id: "wizard.day-schedule.time-picker" }
- assertVisible: { id: "wizard.day-schedule.day-card.0.end-field" }
