appId: ${APP_ID}
name: WZ-C-03
tags: [needs-implementation, wizard, p1]
---
# >>> dashboard-meta
# WZ-C-03 — On Companions, tapping plus on Adults stops at 20
# Section: Plan Creation Wizard
# Priority: P1
# Repro:
# 1. On Companions, tap the plus button on Adults repeatedly until the value reaches 20.
# 2. Verify further taps do not increase the value past 20.
# <<< dashboard-meta

# TODO: implement interaction steps. Remove the `needs-implementation`
# tag above once the flow asserts the expected behavior end-to-end.
- runFlow: ../common/_reset_data.yaml
- runFlow: ../common/_open_app.yaml
