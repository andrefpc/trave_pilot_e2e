appId: ${APP_ID}
name: WZ-WS-05
tags: [needs-implementation, wizard, p1]
---
# >>> dashboard-meta
# WZ-WS-05 — Tapping Next on Walking Style sends PUT /travel-plans/:id/walking-style and opens Companions
# Section: Plan Creation Wizard
# Priority: P1
# Repro:
# 1. On Walking Style, tap "Moderate walking" → Next.
# 2. Verify a PUT request to /travel-plans/:id/walking-style fires (Network Profiler) with body {style: "moderate"}.
# 3. Verify Companions opens.
# <<< dashboard-meta

# TODO: implement interaction steps. Remove the `needs-implementation`
# tag above once the flow asserts the expected behavior end-to-end.
- runFlow: ../common/_reset_data.yaml
- runFlow: ../common/_open_app.yaml
