appId: ${APP_ID}
name: TK-07
tags: [needs-implementation, tickets, p0]
---
# >>> dashboard-meta
# TK-07 — Tapping Delete on a ticket → Confirm sends DELETE /tickets/:id and removes the ticket
# Section: Tickets
# Priority: P0
# Repro:
# 1. Open a ticket full-screen and tap Delete.
# 2. Confirm in the dialog.
# 3. Verify a DELETE /tickets/:id fires; the ticket disappears and a "Ticket deleted" snackbar appears.
# <<< dashboard-meta

# TODO: implement interaction steps. Remove the `needs-implementation`
# tag above once the flow asserts the expected behavior end-to-end.
- runFlow: ../common/_reset_data.yaml
- runFlow: ../common/_open_app.yaml
